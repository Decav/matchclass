// Template: index.ts — Barrel export del módulo
// Solo exporta lo que otros módulos o el router necesitan.
// Los componentes internos NO se exportan aquí.

// Rutas (para el router)
export { exampleRoutes } from './routes/example.routes';

// Tipos públicos (si los necesitan otros módulos)
// export type { Example } from './core/types/example.types';
